apiVersion: v1
kind: ConfigMap
metadata:
  name: todo-config
  namespace: todo-app
  labels:
    app.kubernetes.io/name: todo-app
data:
  CORS_ORIGINS: "http://localhost:3000,http://todo.local"
  OPENAI_MODEL: "gpt-4o-mini"
